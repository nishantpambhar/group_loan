name: group_loan_app
description: Group savings & loan circle manager (bachat gat).
publish_to: "none"
version: 1.0.3+4

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.5
  intl: ^0.20.2
  google_fonts: ^8.1.0
  share_plus: 10.1.4
  path_provider: ^2.1.5
  pdf: ^3.11.3
  firebase_core:
  firebase_auth:
  cloud_firestore:

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
